{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://understand-anything/understand-link/manifest.schema.json",
  "title": "understand-link manifest",
  "description": "Hand-maintained service inventory for cross-service graph linking (DESIGN.md §13 decision ①). The single entry point: lists every service, where its subgraphs live, and the HTTP normalization context the extractor cannot infer.",
  "type": "object",
  "required": ["services"],
  "additionalProperties": false,
  "properties": {
    "version": { "type": "string", "description": "Manifest schema version." },
    "registry": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "backend": {
          "type": "string",
          "enum": ["json", "sqlite"],
          "default": "json",
          "description": "Registry backend (DESIGN.md §5.2 / decision ⑤). \"json\" (default): single git-diffable registry.json. \"sqlite\": a real SQLite file (via Node's built-in node:sqlite) for scale or to let an MCP/AI run SQL over the boundary index. Both produce identical cross-edges."
        }
      }
    },
    "services": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/definitions/service" }
    }
  },
  "definitions": {
    "service": {
      "type": "object",
      "required": ["serviceId", "graphRef", "domainRef"],
      "additionalProperties": false,
      "properties": {
        "serviceId": {
          "type": "string",
          "description": "Stable unique id for the service (used as the node id in the system graph)."
        },
        "repo": {
          "type": "string",
          "description": "Repository name / label. Defaults to serviceId."
        },
        "root": {
          "type": "string",
          "description": "Path to the service source root to scan for .java files. Relative paths resolve against the manifest's directory. Defaults to repo or '.'."
        },
        "graphRef": {
          "type": "string",
          "description": "Path to the service's knowledge-graph.json (hard prerequisite — run /understand). Relative to the manifest's directory."
        },
        "domainRef": {
          "type": "string",
          "description": "Path to the service's domain-graph.json (hard prerequisite — run /understand-domain). Relative to the manifest's directory."
        },
        "http": {
          "type": "object",
          "description": "HTTP route normalization context the static scan cannot infer (DESIGN.md §6 / §13 decision ①).",
          "additionalProperties": false,
          "properties": {
            "basePath": {
              "type": "string",
              "description": "Service base path prepended to controller routes, e.g. \"/aurora\"."
            },
            "gatewayPrefix": {
              "type": "string",
              "description": "Gateway prefix prepended ahead of basePath, e.g. a sisyphus gateway route prefix."
            },
            "hostMap": {
              "type": "object",
              "description": "For frontend services: maps the host variable used in `url: `${host}/path`` (destructured from process.env.API_HOST_LIST) to the target service's base path, e.g. { \"aurora\": \"/aurora\" }. Used to reconstruct the absolute route so fe consumer keys match backend provider keys (DESIGN.md §6.1). Unmapped hosts keep the bare path and fall to unresolved.",
              "additionalProperties": { "type": "string" }
            }
          }
        },
        "mq": {
          "type": "object",
          "description": "MQ extractor config for self-wrapped RocketMQ (DESIGN.md §6.1). Override only when a service names its base/send classes differently, or supply topic values the static scan cannot infer.",
          "additionalProperties": false,
          "properties": {
            "consumerBaseClass": {
              "type": "string",
              "description": "Simple name of the MQ consumer base class. Default \"AbstractRocketMqHandler\"."
            },
            "producerClass": {
              "type": "string",
              "description": "Simple name of the MQ send service whose .send(topic, …) marks a producer. Default \"MqSendService\"."
            },
            "topics": {
              "type": "object",
              "description": "Apollo/config-sourced topic values the Java source does not contain (DESIGN.md §13, verified against aurora-service). The user supplies the mapping, mirroring how the HTTP gateway prefix is hand-provided.",
              "additionalProperties": false,
              "properties": {
                "byProp": {
                  "type": "object",
                  "description": "Producer side: `@Value(\"${KEY}\")` property key → real topic name, e.g. { \"mq.rocket-mq.producerTopic_dispatchAnalysisNoticed\": \"DISPATCH_ANALYSIS_NOTICED\" }.",
                  "additionalProperties": { "type": "string" }
                },
                "byHandler": {
                  "type": "object",
                  "description": "Consumer side: handler class FQN → real topic name, e.g. { \"com.hk.simba.pre.mq.consumer.PreDispatchAnalysisNoticedHandler\": \"DISPATCH_ANALYSIS_NOTICED\" }.",
                  "additionalProperties": { "type": "string" }
                }
              }
            }
          }
        },
        "protocols": {
          "type": "object",
          "description": "Per-protocol extraction switches. Omitted protocols default to enabled. v0.2 supports dubbo + http (Java providers + fe consumers) + mq.",
          "additionalProperties": false,
          "properties": {
            "dubbo": { "type": "boolean", "default": true },
            "http": { "type": "boolean", "default": true },
            "mq": { "type": "boolean", "default": true }
          }
        }
      }
    }
  }
}
